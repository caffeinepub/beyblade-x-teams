{
  "kind": "build_request",
  "title": "Fix Actor not available error when creating teams",
  "priority": "high",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Debug and fix the 'Actor not available' error that occurs when users attempt to create a new team through the CreateTeamPage",
      "target": "both",
      "source": {
        "messageIds": [
          "msg-user-actor-error"
        ],
        "quotes": [
          "when I try and make a new team, it says \"Actor not available\""
        ]
      },
      "acceptanceCriteria": [
        "Users can successfully create new teams without encountering 'Actor not available' error",
        "Team creation mutation completes successfully and navigates to the team detail page",
        "Backend actor is properly initialized and accessible from the CreateTeamPage component"
      ]
    },
    {
      "id": "REQ-2",
      "text": "Verify that the backend actor initialization in useActor hook properly handles authentication state and access control setup",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-actor-error"
        ]
      },
      "acceptanceCriteria": [
        "Actor initialization completes before any mutations are attempted",
        "Proper error handling displays meaningful messages if actor initialization fails",
        "Authentication state is verified before allowing team creation"
      ]
    },
    {
      "id": "REQ-3",
      "text": "Add error boundary or loading state handling in CreateTeamPage to prevent premature mutations before actor is ready",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-actor-error"
        ]
      },
      "acceptanceCriteria": [
        "Team creation form is disabled until actor is confirmed available",
        "Clear loading indicator shows when actor is initializing",
        "User-friendly error message displays if actor fails to initialize"
      ]
    }
  ],
  "constraints": [
    "Do not change the existing authentication flow or Internet Identity integration",
    "Preserve the current team creation data model and backend API structure",
    "Maintain compatibility with existing useQueries hooks and React Query cache invalidation"
  ],
  "nonGoals": [
    "Redesigning the team creation UI or form layout",
    "Adding new team creation features or fields",
    "Modifying the backend actor architecture beyond fixing the availability issue"
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}